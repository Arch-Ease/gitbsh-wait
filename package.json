{
  "name": "gitbsh-wait",
  "version": "0.1.0",
  "private": true,
  "description": "Static demo dashboard for a developer productivity and GitHub alternative simulation.",
  "scripts": {
    "dev": "python3 -m http.server 4173 --bind 127.0.0.1",
    "build": "node scripts/validate-demo.mjs",
    "lint": "node --check app.js && node scripts/validate-demo.mjs"
  }
}
